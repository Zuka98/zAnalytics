# Enforce engine versions from package.json
engine-strict=true

# Hoist everything so workspace packages resolve each other
node-linker=hoisted

# GitHub Packages registry for your scope (uncomment and replace @scope)
# @scope:registry=https://npm.pkg.github.com
# //npm.pkg.github.com/:_authToken=${GITHUB_TOKEN}
